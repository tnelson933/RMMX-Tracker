{
  "name": "@workspace/connector",
  "version": "1.0.5",
  "private": true,
  "description": "RM Connect \u2014 lightweight tray app that bridges timing hardware to the cloud",
  "main": "dist/main.js",
  "scripts": {
    "build": "node build.mjs",
    "start": "pnpm run build && electron .",
    "dist": "pnpm run build && electron-builder",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/ws": "^8.5.12",
    "electron": "^33.3.1",
    "electron-builder": "^25.1.8",
    "esbuild": "0.27.3",
    "typescript": "^5.9.3"
  }
}
